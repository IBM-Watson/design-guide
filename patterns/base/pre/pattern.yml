name: Pre
description: Pre formatted text
displayTemplate: pattern
variables:
  snippet: >
    h1 {
      font-size: ms(5);
      font-weight: normal;

      @include breakpoint($large-typography) {
        font-size: ms(6);
      }
    }
